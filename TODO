Общее
=====
- сделать автоматическое подключение "модулей"
  (наприрмер проходим по спец. каталогу и добавлем все модули в нём)
- сделать по каждому настриваемому параметру вывод "подсказки"
- доделать механизм переводов (gettext)
+ вынести общее в диалогах dlg_slist и dlg_nodes
  сделал универсальный dlg_xlist
- сделать, чтобы срабатывал ENTER и ESC в диалогах
- сделать описание форматов по каждому модулю
  (т.е. описать как хранится в xml)
- продумать взаимодействие настроек в xml со скриптами запуска
(может генерировать скрипты редактором?)
- подумать можно ли xml properties сохранять не в алфафитном порядке (как сейчас),
 а в заданном (это удобно, что-бы настроки в/в отличать от can, общих и т.п.)
+ в диалоге dlg_slist сделать фильтр на AI,AO,DI,DO
    Т.к. сделан унивесальный dlg_list, то пока сделан фильтр по name, textname, id

I/O
===
+ проверка на дублирование привяки 
(попытка привязать один и тот же датчик к двум разным каналам)
- обдумать надо ли проверять корректности привязки (AI --> к AI и т.п.)
+ открытие файла из командной строки или через диалог
- глобальный поиск (по имени датчика)
  + локальный в списке уже есть - стандартный по Ctl+S
- добавить значки (частично можно добавлять прямо в glade)
+ убрать лишние меню
- разобраться с начальным размером диалога выбора датчика
+ добавление и удаление карт
- drag and drop (для копирования карт между узлами)
+ настройка параметров для карты в/в (базовый адрес, прерывание и т.п.)
+ проверять ли "повторность" номера карты и базовый адрес при внесении новой карты
  (Причём как "цифры", а не "текста")
+ сделать автоматический "форматер" xml-файла (чтобы читабельно выглядел)
  Не очень "изящно", но работает
- в диалоге добавления карт, сделать, чтобы OK срабатывала на ENTER
+- реструктурировать код... (вынести одинаковый код в функции, может что-то выделить
в отдельные классы)
-  сделать хранение настроек пользователя (наверно в xml) и соответствующее меню
   - сохранять ли файл в формате "pretty save"
   - ...
- ввести поле "module" (driver)?
- продумать какие поля сделать обязательными для задания
- проверка на корректность привязок при загруке файла
- продумать может для разных типов карт есть ограничения по базовому адресу
  (соответсственно можно сделать проверку)
- Для некоторых типов карт есть ещё "прерывание" (ну и вообще спец. настройки)
  Продумать механизм динамического формирования диалога настроек.
+ настройка ВСЕХ параметров поддтерживаемых IOControl (rmin,rmax,cdiagramm,...)
+ перевести содание всплывающих меню тоже в glade
- "Threshold"  - настройка не является настройкой для карт в/в (!)
  Надо либо убирать из диалога и создавать отдельный настройщик "программных"
  пороговых датчиков. Либо что-то ещё..
- сделать предупреждение, если выставляется iotype не соответствующий карте
+ Реализовать обработку сигналов по изменению списка узлов

CAN
===
- подхват имени при вводе датчика в Entry (сейчас сделано через выбор "...")
+ функции удаления, добавления, редактирования узла
+ функции удаления, добавления, редактирования сети
- продумать какие параметры сделать обязательными
+ сделать проверку nodeID как число.. а не как строку..
   - пока сделал SpinButton для задания...
- надо ли делать проверку, что название подсетей задаётся латинскими буквами
  (добавить поле comment для русскоязычного коментария, если надо)
- ПРОДУМАТЬ механизм добавления датчиков (multiselect в dlg_slist?)
- чтобы "удобно" видеть какие сети есть и кто в них входит,
  выводить "Сеть: список узлов" хотя-бы в виде коментария перед <nodes>
- Поле "comment" вообще-то не нужно, потому-что получается, что 
  он (один и тот же) прописывается у каждого узла входящего в данную сеть
- Реализовать обработку сигналов по изменению списка узлов

Nodes
=====
+ изменение в списке узлов влияет на модуль i/o и can (!)
  надо либо придумать сигнал (изменение в списке узлов) на этот случай.. 
  либо что-то ещё..
  Сделаны сигналы: на удаление, добавление и редактирование

Sensors
=======
- создать редактор

Objects
=======
- создать редактор

UniSet parameters
=================
- создать редактор

Unet
====
- создать редактор

ModbusTCP
=========
- создать редактор

ModbusRTU
=========
- создать редактор

SM
=========
- создать редактор (если возможно и нужно)

